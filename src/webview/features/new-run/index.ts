/**
 * Public API фичи `new-run` (#0018). Главные сценарии main-area:
 *  - `NewRunForm` — экран создания нового рана (заголовок + запрос).
 *  - `MainEmptyState` — приглашение запустить первый ран.
 */
export { NewRunForm } from './ui/NewRunForm';
export { MainEmptyState } from './ui/MainEmptyState';
