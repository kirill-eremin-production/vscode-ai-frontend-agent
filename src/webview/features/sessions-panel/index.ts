/**
 * Public API фичи `sessions-panel` (#0019). Правый сайдбар с деревом
 * сессий выбранного рана; AppShell рендерит компонент без пропсов —
 * collapsed-state и выбранный ран читаются из общего store.
 */
export { SessionsPanel } from './ui/SessionsPanel';
