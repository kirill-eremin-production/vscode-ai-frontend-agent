import { AppShell } from './shell/AppShell';

/**
 * Корневой компонент webview-приложения.
 *
 * По FSD слой `app` отвечает за инициализацию и сборку всего, что
 * должно быть «глобальным» для приложения: провайдеры контекста,
 * темы, error boundary, роутер и т.п. Сейчас таких сущностей нет,
 * поэтому `App` — это просто обёртка над shell'ом (#0017).
 *
 * Когда здесь появятся провайдеры — оборачиваем `AppShell` в них
 * именно тут, чтобы фичи и shell оставались чистыми от знания
 * о глобальном инфраструктурном окружении.
 */
export function App() {
  return <AppShell />;
}
