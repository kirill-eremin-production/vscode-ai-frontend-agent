/**
 * Шаблон `README.md` для kb программиста (`.agents/knowledge/programmer/README.md`).
 *
 * Аналог [architect-readme](./architect-readme.ts) и
 * [product-readme](./product-readme.ts) — описание схемы как для
 * человека, так и для самой роли. Хранится строкой по тем же причинам.
 *
 * Имена поддиректорий ДОЛЖНЫ совпадать с `PROGRAMMER_SUBDIRS` из
 * [schema.ts](./schema.ts) — при правках обе стороны меняем синхронно.
 */

export const PROGRAMMER_KB_README_MARKDOWN = `# База знаний программиста

Это персистентная память роли-реализатора: накопленные паттерны кодовой
базы, реализационные решения по ходу ранов и грабли. Файлы здесь живут
вместе с кодом проекта и коммитятся в git.

## Структура

\`\`\`
programmer/
  patterns/   — устоявшиеся паттерны проекта (как пишутся тесты,
                как структурируем фичи, как делаем IPC, и т.п.)
  decisions/  — реализационные решения с датой (формат ADR)
  gotchas/    — грабли и обходные пути: флаки тесты, странности
                тулинга, кросс-платформенные сюрпризы
  summaries/  — отчёты по ранам (\`<runId>-<slug>.md\`), пишутся
                специальным финализатором — руками не правим
\`\`\`

Список поддиректорий **закрытый**. Свободные папки запрещены — расширение
схемы делается кодом, см. \`src/extension/entities/knowledge/schema.ts\`.

## Имена файлов

- \`patterns/<pattern-slug>.md\` — slug в kebab-case, по одному паттерну.
- \`decisions/YYYY-MM-DD-<slug>.md\` — дата решения первой, чтобы
  хронология читалась по сортировке.
- \`gotchas/<gotcha-slug>.md\` — короткий slug с сутью граблей.

Только markdown.

## Фронтматтер

Каждый файл начинается с YAML-фронтматтера:

\`\`\`yaml
---
type: pattern | decision | gotcha
created: YYYY-MM-DD
updated: YYYY-MM-DD
related: [список путей внутри kb, например "patterns/testing.md"]
---
\`\`\`

Поле \`type\` обязано совпадать с поддиректорией. \`related\` — список
относительных путей от корня kb программиста; пустой массив допустим.

## Граница с архитектором и продактом

**Запрещено** читать или ссылаться на \`knowledge/product/\` и
\`knowledge/architect/\`. Связь между ролями — только через артефакты
ранов (\`brief.md\` ты не получаешь напрямую, \`plan.md\` приходит как
стартовое user-сообщение). Если для реализации не хватает контекста —
задай уточнение через \`ask_user\`, не лезь в чужую kb.

## Как искать

- \`kb.list\` — посмотреть содержимое поддиректории (без рекурсии).
- \`kb.grep\` — поиск по содержимому (фильтрация по \`type:\` через
  \`pattern: "^type: pattern"\`).
- \`kb.read\` — прочитать конкретный файл целиком.
`;
