/**
 * Шаблон `README.md` для продактовой kb (`.agents/knowledge/product/README.md`).
 *
 * Что это и зачем:
 *  - **Описание схемы для человека.** Кто-то открывает папку
 *    `.agents/knowledge/product/` в репо и понимает, что куда класть.
 *  - **«System-prompt extension» для роли.** Продакт (#0003) читает
 *    этот файл в начале каждого рана, чтобы помнить правила. Поэтому
 *    текст пишется как инструкция модели, а не как читалка для людей,
 *    но достаточно нейтрально, чтобы быть полезным и тем и другим.
 *  - **Источник правды о схеме.** Имена поддиректорий и формат
 *    фронтматтера в этом файле должны совпадать с `KNOWLEDGE_SCHEMA`
 *    из `schema.ts`. При расхождении — правим обе стороны (тест в
 *    `product-readme.test.ts` ловит десинхронизацию имён директорий).
 *
 * Хранится строкой в .ts, а не отдельным .md, потому что:
 *  - bundler (esbuild) собирает extension в один файл — читать .md
 *    из disk через `fs.readFile` потребовало бы помнить про
 *    bundler-копирование ассетов, а это лишняя ось сложности;
 *  - текст версионируется в git и diff'ится наравне с кодом.
 *
 * Не путать с `README.md` в `.agents/knowledge/product/` — туда этот
 * текст когда-нибудь запишет init-скрипт продакта (#0003 решит как).
 */

export const PRODUCT_KB_README_MARKDOWN = `# База знаний продакта

Это персистентная память продуктового агента. Файлы здесь живут вместе с
кодом и коммитятся в git — продуктовая память это часть проекта, а не
личное хранилище агента.

## Структура

\`\`\`
product/
  glossary/    — определения продуктовых терминов (по файлу на термин)
  personas/    — описания целевых пользователей
  decisions/   — продуктовые решения с датой (формат ADR)
  features/    — описания фич/областей продукта + обобщённый опыт прошлых ранов
  questions/   — открытые вопросы, ещё не закрытые ответом пользователя
\`\`\`

Список поддиректорий **закрытый**. Если файл не подходит ни в одну —
сначала договорись с пользователем, какая папка нужна, и расширь схему
кодом (см. \`src/extension/entities/knowledge/schema.ts\` в репо
расширения), потом уже клади файл. Свободные папки запрещены.

## Имена файлов

- \`glossary/<term-slug>.md\` — slug в kebab-case, по одному термину.
- \`personas/<persona-slug>.md\` — один файл на персону.
- \`decisions/YYYY-MM-DD-<slug>.md\` — дата принятия решения первой,
  чтобы хронология читалась по сортировке.
- \`features/<feature-slug>.md\` — slug фичи в kebab-case.
- \`questions/<question-slug>.md\` — короткий slug вопроса.

Только markdown, никаких других расширений.

## Фронтматтер

Каждый файл начинается с YAML-фронтматтера:

\`\`\`yaml
---
type: glossary | persona | decision | feature | question
created: YYYY-MM-DD
updated: YYYY-MM-DD
related: [список путей внутри kb, например "features/auth.md"]
---
\`\`\`

Поле \`type\` обязано совпадать с поддиректорией. \`related\` — список
относительных путей от корня kb (\`.agents/knowledge/\`); пустой массив
допустим, если связей нет.

## Жизненный цикл вопросов

Файл из \`questions/\` **никогда не удаляется**. Когда вопрос закрыт:
- если он привёл к продуктовому решению — переноси содержимое в
  \`decisions/YYYY-MM-DD-<slug>.md\`;
- если ответ касается конкретной фичи — переноси/мерджи в
  \`features/<feature-slug>.md\`.

Это гарантирует, что историю обсуждения можно поднять из git, даже
если конкретный файл переехал.

## Как искать

- \`kb.list\` — посмотреть содержимое поддиректории (без рекурсии).
- \`kb.grep\` — поиск по содержимому. Поскольку \`type:\` лежит во
  фронтматтере, фильтрация по типу делается тем же grep'ом
  (\`pattern: "^type: decision"\`).
- \`kb.read\` — прочитать конкретный файл целиком.

Индексных файлов нет и не будет — индекс быстро устареет и начнёт врать.
`;
