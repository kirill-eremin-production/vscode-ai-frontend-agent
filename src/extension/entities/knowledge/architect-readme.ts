/**
 * Шаблон `README.md` для kb архитектора (`.agents/knowledge/architect/README.md`).
 *
 * Аналог [product-readme](./product-readme.ts) — описание схемы как для
 * человека, так и для самой роли. Хранится строкой по тем же причинам:
 * extension собирается одним файлом, fs-доступ к ассетам — лишняя ось
 * сложности.
 *
 * Имена поддиректорий ДОЛЖНЫ совпадать с `ARCHITECT_SUBDIRS` из
 * [schema.ts](./schema.ts) — при правках обе стороны меняем синхронно.
 */

export const ARCHITECT_KB_README_MARKDOWN = `# База знаний архитектора

Это персистентная память архитекторского агента: накопленные решения по
архитектуре проекта, паттерны, описания модулей и известные риски. Файлы
здесь живут вместе с кодом проекта и коммитятся в git.

## Структура

\`\`\`
architect/
  modules/    — описания модулей/слоёв проекта (по файлу на модуль)
  decisions/  — архитектурные решения с датой (формат ADR)
  patterns/   — устоявшиеся паттерны (как делаем IPC, тесты, и т.п.)
  risks/      — известные риски и грабли
\`\`\`

Список поддиректорий **закрытый**. Свободные папки запрещены — расширение
схемы делается кодом, см. \`src/extension/entities/knowledge/schema.ts\`.

## Имена файлов

- \`modules/<module-slug>.md\` — slug в kebab-case, по одному модулю.
- \`decisions/YYYY-MM-DD-<slug>.md\` — дата решения первой, чтобы
  хронология читалась по сортировке.
- \`patterns/<pattern-slug>.md\` — slug паттерна в kebab-case.
- \`risks/<risk-slug>.md\` — короткий slug риска.

Только markdown.

## Фронтматтер

Каждый файл начинается с YAML-фронтматтера:

\`\`\`yaml
---
type: module | decision | pattern | risk
created: YYYY-MM-DD
updated: YYYY-MM-DD
related: [список путей внутри kb, например "modules/auth.md"]
---
\`\`\`

Поле \`type\` обязано совпадать с поддиректорией. \`related\` — список
относительных путей от корня kb архитектора (\`architect/...\`); пустой
массив допустим.

## Граница с продактом

**Запрещено** читать или ссылаться на \`knowledge/product/\`. Если для
плана не хватает продуктового контекста — задай вопрос пользователю
через \`ask_user\`, не лезь в чужую kb. Связь между ролями идёт только
через \`brief.md\`, который ты получаешь как стартовое сообщение.

## Как искать

- \`kb.list\` — посмотреть содержимое поддиректории (без рекурсии).
- \`kb.grep\` — поиск по содержимому (фильтрация по \`type:\` через
  \`pattern: "^type: decision"\`).
- \`kb.read\` — прочитать конкретный файл целиком.
`;
